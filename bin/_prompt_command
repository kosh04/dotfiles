#!/bin/bash
# Usage: source ~/path/to/_prompt_command
# See also: /etc/bash.bashrc

case "$TERM" in
xterm*|rxvt*)
    PROMPT_COMMAND='echo -ne "\e]1;${USER}@${HOSTNAME}: ${PWD}\a"'
    ;;
*)
    ;;
esac
